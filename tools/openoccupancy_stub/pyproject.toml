[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "OpenOccupancy"
version = "0.0"
description = "Stub package to satisfy EDEN requirements. No functionality."
authors = [{ name = "stub" }]
dependencies = []
requires-python = ">=3.8"


